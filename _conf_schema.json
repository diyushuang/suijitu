{
  "apiUrl": {
    "description": "随机图床API地址（完整URL，包含路径）",
    "type": "string",
    "hint": "请输入完整的随机图床API地址，例如：https://cfbed.sanyue.de/api/random.html 或 https://api.oick.cn/random/api.php",
    "default": "https://example.com"
  },
  "timeout": {
    "description": "API请求超时时间（秒）",
    "type": "int",
    "hint": "设置API请求的超时时间，默认为10秒",
    "default": 10
  },
  "retryCount": {
    "description": "API请求重试次数",
    "type": "int",
    "hint": "设置API请求失败后的重试次数，默认为3次",
    "default": 3
  }
}
